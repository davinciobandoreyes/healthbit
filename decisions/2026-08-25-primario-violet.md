# Primario violet / indigo (morado azulado)

**Qué:** El CTA y los acentos de marca dejan de ser emerald/teal. Primario = `violet-600` (hover `violet-700`). Companion = `indigo`. Pendiente sigue en ámbar; canvas en `slate-50`.

**Por qué:** Pedido explícito de producto: sustituir el verde por un morado azulado. Se usan escalas Tailwind existentes (no tokens inventados).

**Vigencia:** vigente. Anula el CTA `emerald-600` de la paleta anterior.
